<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ebook"
        tools:replace="android:appComponentFactory"
        tools:targetApi="31">

        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Deklarasi untuk Activity Admin -->
        <activity
            android:name=".AdminActivity"
            android:exported="false" />

        <!-- Deklarasi untuk Fitur Ubah Tampilan -->
        <activity
            android:name=".UbahTampilanActivity"
            android:exported="false" />

        <!-- Deklarasi untuk Fitur Kelola Meditasi -->
        <activity
            android:name=".KelolaFiturActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".MeditasiActivity"
            android:exported="false" />
        <activity
            android:name=".JurnalActivity"
            android:exported="false" />
        <activity
            android:name=".RiwayatJurnalActivity"
            android:exported="false" />
        <activity
            android:name=".StepMeditasiActivity"
            android:exported="false" />
        <activity
            android:name=".DetailMeditasiActivity"
            android:exported="false" />
        <activity
            android:name=".EbookActivity"
            android:exported="false" />

    </application>

</manifest>
